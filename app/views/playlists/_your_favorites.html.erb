<div id="your_favorites" class="playlist_source">
  <div class="mini_paginator">
    <%= will_paginate @favorites, {:param_name => :favorites_page, :ignore_get_params => true}%>
  </div>
	
	<div class="scriptaculous">
	  <%= render @favorites.collect(&:asset) %>
	</div>
</div>
